services:
  app:
    build:
      context: .
      dockerfile: app/Dockerfile
    container_name: r_auto_app
    volumes:
      - ./app:/workspace/app # ソースコードをマウント
      - ./db:/workspace/db   # DBデータをマウント
      - ./web:/workspace/web # Webリソースをマウント
      - ./app/entrypoint.sh:/usr/local/bin/entrypoint.sh  # ←追加！
    ports:
      - "8000:8000" # Web UIアクセス用にポートを開放
      - "5900:5900" # VNC接続用にポートを開放
    environment:
      - TZ=Asia/Tokyo # タイムゾーンを日本時間に設定
    env_file:
      - .env
    # command: python -m app.main # entrypoint.shで起動するため不要
    tty: true
    
# まだWeb/Dockerfileがない場合はコメントアウト
#  web:
#    build: ./web
#    container_name: r_auto_web
#    ports:
#      - "5000:5000"
#    volumes:
#      - ./web:/workspace/web
#    env_file:
#      - .env
#    depends_on:
#      - app
